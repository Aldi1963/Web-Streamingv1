"use client";
import { useEffect,useState } from "react";
import { Plus,Save,X } from "lucide-react";
const empty={name:"",slug:"",price:0,durationDays:30,isTrial:false,isActive:true};
export function AdminPlans(){const[plans,setPlans]=useState<any[]>([]);const[editing,setEditing]=useState<any|null>(null);const[message,setMessage]=useState("");const load=()=>fetch("/api/admin/plans").then(r=>r.json()).then(setPlans);useEffect(()=>{void load()},[]);
async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const response=await fetch("/api/admin/plans",{method:editing.id?"PATCH":"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(editing)});const result=await response.json();setMessage(result.message);if(response.ok){setEditing(null);void load()}}
return <div className="resource-page"><div className="resource-toolbar"><p>{plans.length} paket tersedia</p><button className="btn" onClick={()=>setEditing({...empty})}><Plus size={17}/>Paket baru</button></div>{message&&<div className="panel">{message}</div>}<div className="plan-admin-grid">{plans.map(plan=><article className={`panel plan-admin-card${plan.isActive?"":" inactive"}`} key={plan.id}><div><span className={`status-badge ${plan.isActive?"success":"danger"}`}>{plan.isActive?"Aktif":"Nonaktif"}</span><h2>{plan.name}</h2><strong>Rp {Number(plan.price).toLocaleString("id-ID")}</strong><p>Akses seluruh episode selama {plan.durationDays} hari</p><small>{plan._count.subscriptions} langganan</small></div><button className="btn btn-secondary btn-sm" onClick={()=>setEditing({...plan,price:Number(plan.price)})}>Edit</button></article>)}</div>
{editing&&<div className="dialog-overlay" onClick={()=>setEditing(null)}><form className="dialog-card plan-form" onSubmit={save} onClick={e=>e.stopPropagation()}><div className="form-title"><h2>{editing.id?"Edit paket":"Paket baru"}</h2><button type="button" onClick={()=>setEditing(null)}><X/></button></div><label>Nama<input value={editing.name} onChange={e=>setEditing({...editing,name:e.target.value})} required/></label><label>Slug<input value={editing.slug} onChange={e=>setEditing({...editing,slug:e.target.value.toLowerCase().replace(/[^a-z0-9-]/g,"-")})} required/></label><div className="form-grid"><label>Harga<input type="number" min="0" value={editing.price} onChange={e=>setEditing({...editing,price:Number(e.target.value)})}/></label><label>Durasi akses (hari)<input type="number" min="1" value={editing.durationDays} onChange={e=>setEditing({...editing,durationDays:Number(e.target.value)})}/></label></div><label className="check"><input type="checkbox" checked={editing.isTrial} onChange={e=>setEditing({...editing,isTrial:e.target.checked})}/>Paket trial</label><label className="check"><input type="checkbox" checked={editing.isActive} onChange={e=>setEditing({...editing,isActive:e.target.checked})}/>Aktif</label><button className="btn"><Save size={17}/>Simpan paket</button></form></div>}</div>}
